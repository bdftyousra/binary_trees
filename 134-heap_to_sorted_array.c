#include "binary_trees.h"
/**
 * heap_to_sorted_array - converts a Binary Max Heap to a sorted array of int
 * @heap: root of the tree
 * @size: size from array
 * Return: pointer
 */
int *heap_to_sorted_array(heap_t *heap, size_t *size)
{
	return (NULL);
}
