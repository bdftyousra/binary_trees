#include "binary_trees.h"
/**
 * heap_insert -function insert node in a heap tree
 * @root: root of the tre to evaluate
 * @value: value to insert
 * Return: pointer
 */
heap_t *heap_insert(heap_t **root, int value)
{
	return (NULL);
}
